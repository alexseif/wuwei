{% extends 'base.html.twig' %}

{% block h1 %}Payments{% endblock %}
{% block card_actions %}
	{{ include('components/Link/New.html.twig', {'name':'payments'}) }}
{% endblock %}
{% block body %}
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>Due_date</th>
                <th>Amount</th>
                <th>Status</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for payment in payments %}
            <tr>
                <td>						{{ include('components/Link/Entity.html.twig', {'name': 'payments', 'entity': payments}) }}
</td>
                <td>{{ payment.description }}</td>
                <td>{{ payment.dueDate ? payment.dueDate|date('Y-m-d') : '' }}</td>
                <td>{{ payment.amount }}</td>
                <td>{{ payment.status }}</td>
                <td>
                                        						{{ include('components/Link/Edit.html.twig', {'name': 'requirements', 'id': requirements.id}) }}

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
