<form method="post" action="{{ path('app_daily_delete', {'id': daily.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ daily.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
