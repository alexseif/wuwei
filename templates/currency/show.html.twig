{% extends 'base.html.twig' %}

{% block h1 %}Currency:
	{{ currency.code }}
	-
	{{ (currency.EGP/100)|format_currency('EGP') }}
{% endblock %}
{% block card_actions %}
	{{ include('components/Link/Back.html.twig', {'name': 'currency'}) }}
	{{ include('components/Link/Edit.html.twig', {'name': 'currency','id': currency.id}) }}
	{{ include('currency/_delete_form.html.twig') }}
{% endblock %}
{% block body %}
	<div class="card-footer text-end">
		{{ include('components/Badge/Timestamps.html.twig', {'entity': currency}) }}
	</div>
{% endblock %}
