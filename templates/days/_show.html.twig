{{ include('components/Day/Day.html.twig') }}

<table class="table table-striped">
	<tbody>
		<tr>
			<th>Completed:</th>
			<td>
				{{ include('components/Badge/Completed.html.twig', {completed: day.complete})}}
				{{ day.complete ? 'Yes' : 'No' }}
			</td>
		</tr>
	</tbody>
</table>
<div class="text-end">
	{{ include('components/Badge/Timestamps.html.twig', {'entity': day}) }}
</div>
<div class="btn-toolbar">
	<button class="btn btn-outline-success">
		Complete
	</button>
	{{ include('components/Link/Back.html.twig', {'name': 'days', text: 'Days'}) }}
	{{ include('components/Link/Edit.html.twig', {'name': 'days', 'id': day.id}) }}
	{{ include('days/_delete_form.html.twig') }}
</div>
