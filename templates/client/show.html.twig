{% extends 'base.html.twig' %}
{% block card_header %}
	<div class="card-header d-flex align-items-center justify-content-between">
		<h1 class="card-title me-5">
			Client: #{{ client.id }}
			{{ client.name }}
		</h1>
		<div class="card-content d-flex flex-grow-1">
			{{ include('components/Badge/Enabled.html.twig', {'enabled': client.enabled}) }}
			{% for name,option in client.billingOption %}
				{{ include('components/Badge.html.twig', {'text': name ~ ': ' ~ option, 'attrs':{'title': name}}) }}
			{% endfor %}
		</div>
		<div class="card-actions btn-toolbar ms-5">
			{{ include('components/Link/Back.html.twig', {'link': path('app_client_index') }) }}
			{{ include('components/Link/Edit.html.twig', {'link': path('app_client_edit', {'id': client.id}) }) }}
			{{ include('client/_delete_form.html.twig') }}
		</div>
	</div>
	<!-- .card-header -->
{% endblock %}


{% block body %}
	<div class="card-body">
		<h2>Accounts</h2>
		{% include 'accounts/_table.html.twig' with {'accounts': client.accounts} %}
	</div>
	<div class="card-footer text-end">
		{{ include('components/Badge/Timestamps.html.twig', {'entity': client}) }}
	</div>
{% endblock %}
