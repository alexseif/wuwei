{% extends 'base.html.twig' %}

{% block title %}ItemList{% endblock %}

{% block body %}
    <h1>ItemList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item_list.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ item_list.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_item_list_index') }}">back to list</a>

    <a href="{{ path('app_item_list_edit', {'id': item_list.id}) }}">edit</a>

    {{ include('item_list/_delete_form.html.twig') }}
{% endblock %}
