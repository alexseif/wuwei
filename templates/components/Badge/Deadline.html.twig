{{ include('components/Badge.html.twig', {'text': day.deadline|time_diff, 'class': class|default("badge" ~ (day.deadline and day.deadline < 'now'|date_modify('+3 days')) ? 'bg-danger' : (day.deadline and day.deadline < 'now'|date_modify('+2 weeks')) ? 'bg-warning' : 'bg-secondary') }) }}
{{ include('components/Badge.html.twig', {'text': day.deadline|date('Y-m-d'), 'class': class|default("badge " ~ (day.deadline and day.deadline < 'now'|date_modify('+3 days')) ? 'text-danger' : (day.deadline and day.deadline < 'now'|date_modify('+2 weeks')) ? 'text-warning' : 'text-secondary') }) }}
