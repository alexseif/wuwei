{{ include('components/Badge.html.twig', {'text': day.deadline|time_diff, class: "badge" ~ (day.deadline and day.deadline < 'now'|date_modify('+3 days')) ? 'text-danger' : (day.deadline and day.deadline < 'now'|date_modify('+2 weeks')) ? 'text-warning' : 'text-secondary' }) }}
{{ include('components/Badge.html.twig', {'text': day.deadline|date('Y-m-d'), class: "badge" ~ (day.deadline and day.deadline < 'now'|date_modify('+3 days')) ? 'text-danger' : (day.deadline and day.deadline < 'now'|date_modify('+2 weeks')) ? 'text-warning' : 'text-secondary' }) }}
