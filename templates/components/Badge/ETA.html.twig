{% set textClass =  (eta and eta < ('now'|date_modify('+3 days'))) ? 'text-danger' : (eta and eta < 'now'|date_modify('+2 weeks')) ? 'text-warning' : 'text-secondary' %}
{{ include('components/Badge.html.twig', {'text': (eta) ? eta|date('H:i Y-m-d') ~ "||" ~ eta|time_diff, 'class': textClass, icon: 'bi bi-calendar', 'attrs': {'title': 'ETA'} }) }}

