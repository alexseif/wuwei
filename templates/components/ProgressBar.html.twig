<div class="progress {{ class|default('') }}" role="progressbar" aria-label="{{ label }}" aria-valuenow="{{ value }}" aria-valuemin="{{ min|default(0) }}" aria-valuemax="{{ max|default(100) }}">
	<div class="progress-bar bg-info text-dark" style="width: {{ value }}%">{{ value }}%</div>
</div>
