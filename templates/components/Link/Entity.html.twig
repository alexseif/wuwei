{{ include('components/Link.html.twig', {'link': path('app_'~name~'_show', {'id': entity.id}),'text': entity.name, 'class':  class|default('btn btn-sm btn-link') , 'attrs': attrs|default({'title': name|capitalize}) }) }}
